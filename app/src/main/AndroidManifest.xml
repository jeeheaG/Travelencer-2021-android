<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelencer_android_2021">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".AddPlaceActivity"></activity>
        <activity android:name=".NaviActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".NaviActivity" /> &lt;!&ndash; 액션바 제거 &ndash;&gt; -->
        <activity
            android:name=".Splash"
            android:theme="@style/SplashTheme">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".MainActivity" /> <!-- 홈 액티비티 -->
        <activity android:name=".LoginActivity" /> <!-- 로그인 액티비티 -->
        <activity android:name=".RegisterActivity" /> <!-- 회원가입 액티비티 -->
        <activity android:name=".PasswordFindActivity" /> <!-- 비밀번호 찾기 액티비티 -->
        <activity android:name=".PPActivity" /> <!-- 개인정보 보호정책 액티비티 -->
        <activity android:name=".TOSActivity" />
    </application>

</manifest>